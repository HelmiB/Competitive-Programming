#include<bits/stdc++.h>
using namespace std;
#define all(x) (x).begin(),(x).end()
#define sz(x) (int)(x).size()
const long double EPS = 1e-14;
const int maxn = 100500;
const int mod = 1e9 + 7;


int main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    if(1.0 * (double) clock() / CLOCKS_PER_SEC - /*Time elapsed*/ > EPS){
        //do something if t = Time elapsed (ex : 7.3 sec)
        //maybe you're too stupid to solve this problem so exit before TLE, loser.
    }
    

    return 0;
}
